# Google AI
GOOGLE_API_KEY=
# Optional Gemma second pass (injection + topic). Regex always runs first.
# When set, Gemma also confirms manager-copilot messages that matched the
# ops lexicon are still this store — not sports/news/finance with an ops word.
# Leave unset for deterministic / CI runs.
# GEMMA_MODEL=gemma-3-12b-it

# Backend (api-gateway)
BACKEND_URL=http://127.0.0.1:8080

# JWT secret — MUST match the backend's JWT_SECRET exactly. Used to verify
# customer-presented bearer tokens on POST /agent/chat locally (HS512),
# without a network round-trip.
JWT_SECRET=

# Static service API key for internal /agents/{name}/trigger endpoints.
# Not tied to a customer identity — used by schedulers/ops tooling only.
AGENT_TRIGGER_API_KEY=

# Per-agent scoped API credentials (JSON array). Replaces the single shared
# AGENT_TRIGGER_API_KEY above with least-privilege keys. When unset, AGENT_TRIGGER_API_KEY
# becomes one master ("*"-scoped) credential (migration/back-compat path).
# Example:
# AGENT_API_KEYS=[{"key":"sched-...","scopes":["*"]},{"key":"inv-...","scopes":["trigger:inventory_reorder","read:registry"]}]
AGENT_API_KEYS=

# Redis / RabbitMQ
REDIS_URL=redis://127.0.0.1:6379/1
RABBITMQ_URL=amqp://guest:guest@127.0.0.1:5672/

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080
